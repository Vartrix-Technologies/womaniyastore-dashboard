staff account - attendance history not optimised fir mobile view✅
improve the search functionality - add filters to the search parameters✅
Also, give pre-inputted buttons for QR code search
bug - duplicate addition to cart gives success and error toast together✅
QR code is being randomly assigned to products in inventory
After delete unused functionality - the table needs to auto-refresh
The inventory table has placeholder to add first item to inventory on empty values
error upon deleting categories from finance module 
discount reason drop down



The sale badge is not showing on the product modal


___________________________________________________________
Make a sidebar (should not be visiblw to staff but all other profiles). All the module navigation can be shifted to the sidebar and home page can be used to make it interactive with data analytics, quick links, AI feedback like revenue for today, checklist status for today, 

Caching data remains
Make the stats card throughout the application consistent

Inventory page: 
-remove category breakdown and make it an api call button sitting beside add stock lot button, make sure to add date filtering in there
-remove all status filtering button from the table since it already does the same job from the stats cards
-make the stat cards design consistent and premium like we did for stats cards and table in qr-codes module.
-refer the styling and desgin principles we followed in print qr codes modal/dialog/popup to apply the same here on add stock dialog or any other dialog available
- Also do not hard code the colors on pages but let them be modular and fetch them from styles


QR code:
make the generate tab an accordian with a button leading to settings page for prefix generation
remove status filtering from inside tablle since it is already happenifn from the cards

SAles hub:
Vendor tab: add a parameter in the table stating how many sold and how many left

returns: 
Add option to send confirmation via whatsapp

Attendance:
Infinite time if forgot to clock out
Time in calendar duspayed is not ISt while the one displayed in log is in IST

Staff management:
Page reloads when updating any single field
Make the search bar consistent

Checklist:
remove dedicated filter button which do the same job that the stats card-click does

permission to access camera from browser not consistent? 

Future scope: coupon code

lot id displayed after adding clothes to stock - should not
___________________________________________________________


~manage qr prefix button in add stock lot page~  
~qr code generation toast data issue~   
~Fianaces not updating after return~
~scan camera not working on mobile~
~Inventory needs to have mark as damage~
~Inventory needs to have individual item edit~

~staff name in checklist~
~sale by employee data~
~offline it is not working - no inventory~
~returns has clickable 1st coolumn, make it consistent~
~Customer identity page~
~back navigation on sales, add lot page~
~Loading page~
~supabase performance~
~Theme option~
Dashboard Page
sidebar


Visual Polish:

Table container: Shadow, rounded border, card background
Headers: Uppercase, tracking-wider, subtle background, shorter labels
Rows: Gradient backgrounds with smooth hover transitions, proper group states
Borders: Refined border colors, subtle dividers
Professional Details:

Delete button: Fades in on hover (opacity-50 → 100), smooth color transition
Size badges: Pill-shaped with border and muted background
Sale dropdown: Enhanced with ring effects on dots, better spacing, shadow on trigger
Margin warning: Refined amber color scheme, compact design
Typography: Stronger font weights, better hierarchy, tighter tracking on QR codes
Inputs: Focus states with primary ring, better borders, enhanced shadows
Empty states: Em dash (—) instead of hyphen for elegance
Micro-interactions:

200ms transitions on all hover states
Group hover reveals delete button
Focus rings on form inputs
Gradient backgrounds that intensify on hover
The design now matches professional POS systems like Square or Shopify with attention to spacing, typography, and interactive feedback.